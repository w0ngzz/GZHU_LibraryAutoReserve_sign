name: Libs-Docker

on:
  workflow_dispatch:

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout
        uses: actions/checkout@v4
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v2
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      -
        name: Buildx
        run: |
          docker version
          # 登录阿里云镜像仓库
          docker login --username=${{ secrets.DOCKER_USERNAME }} --password=${{ secrets.DOCKER_PASSWORD }} registry.cn-hangzhou.aliyuncs.com
          cd GZHU_LibraryAutoReserve_sign
          # 使用Dockerfile构建镜像
          docker buildx build --platform linux/amd64,linux/arm64,linux/arm/v7 -t registry.cn-hangzhou.aliyuncs.com/wongzz/lib_actions::V1.0.2 --push .
